require('dotenv').config(); const db = require('./src/core/database/mysql'); const repo = require('./src/modules/attendance/attendance.repository'); async function run() { try { const id = await repo.createMissingCheckIn(26, '2026-06-24 11:30:00', null, null, 'missing_checkin'); console.log('Success:', id); await db.query('DELETE FROM attendance WHERE id = ?', [id]); } catch(e) { console.error('Error:', e); } process.exit(0); } run();
